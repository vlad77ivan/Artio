name: Main - Testing

on:
  push:
    branches:
      - 'main'

jobs:
  test:
    name: Test main branch
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v2
      - name: Hello Test
        run: echo "Hello World!"